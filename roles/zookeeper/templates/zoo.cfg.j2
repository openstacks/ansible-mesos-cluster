tickTime=2000
dataDir=/var/lib/zookeeper/
clientPort={{ zookeeper_client_port }}
initLimit=5
syncLimit=2
{% for host in groups['zookeepers'] %}
server.{{ zoo_id}}={{ host }}:{{ zookeeper_leader_port }}:{{ zookeeper_election_port }}
{% set zoo_id= 1 %}
{% endfor %}
